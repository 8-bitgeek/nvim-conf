> This repository is my neovim configure directory which used to sync my neovim config at everywhere.


# Prerequisite

## Debian

```shell
sudo apt install ripgrep fd-find
```

## Mac

```shell
brew isntall ripgrep ffind
```
